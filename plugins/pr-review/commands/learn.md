---
description: Record a review learning into REVIEW.md — a rule to enforce or a finding class to stop flagging, optionally scoped to paths
argument-hint: "[what to remember]"
disable-model-invocation: true
---

Invoke the `pr-review` skill with the Skill tool and follow it exactly.

Mode: `learn`. Argument: "$ARGUMENTS" (empty → derive from the finding the user just
confirmed or dismissed in this conversation).

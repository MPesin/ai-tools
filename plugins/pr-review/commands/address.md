---
description: Work the unresolved review threads on my pull request — verify each claim against the code, fix or push back with evidence, reply on the thread
argument-hint: "[pr-number]"
disable-model-invocation: true
---

Invoke the `pr-review` skill with the Skill tool and follow it exactly.

Mode: `address`. PR argument: "$ARGUMENTS" (empty → the PR for the current branch).

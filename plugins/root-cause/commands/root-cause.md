---
description: Diagnose a bug the systematic way — reproduce, localize, test one hypothesis at a time, state the root cause, then fix with a regression test
argument-hint: "[symptom, failing test, or issue]"
disable-model-invocation: true
---

Invoke the `root-cause` skill with the Skill tool and follow it exactly.

Symptom: "$ARGUMENTS" (empty → the failure discussed in this conversation).

---
description: Check the decisions skill for rot — dead paths, duplicate ids, stale or contradicting records, oversized files — and propose fixes
disable-model-invocation: true
---

Invoke the `decision-log` skill with the Skill tool and follow it exactly.

Mode: `lint`.

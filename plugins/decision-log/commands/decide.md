---
description: Record a decision and its reasoning (with the rejected alternatives) into the decisions skill, after showing the record for approval
argument-hint: "[what was decided and why]"
disable-model-invocation: true
---

Invoke the `decision-log` skill with the Skill tool and follow it exactly.

Mode: `decide`. Argument: "$ARGUMENTS" (empty → draft from the decision reached
in this conversation).

---
description: Generate a thin CLAUDE.md holding only the always-loaded rules the repo-guide skill can't carry — workflow conventions, hard constraints, environment quirks
---

Invoke the `repo-mapper` skill with the Skill tool and follow it exactly.

Mode argument: `init`
